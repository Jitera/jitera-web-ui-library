export enum ResponsiveDevices {
  MOBILE = 'mobile',
  TABLET = 'tablet',
  DESKTOP = 'desktop'
}

// there is no standard for responsive size, we can config based on our need
export const ResponsiveSize = {
  SMALL: 314,
  MEDIUM: 720,
  LARGE: 1280
}
